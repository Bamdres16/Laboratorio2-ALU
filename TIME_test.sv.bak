module TIME_test #(parameter N = 4);

logic [N-1:0] A, B;
logic [N:0] out1, out2, result;
logic [2:0] selector;
logic [6:0] deco;
logic operacion;
input clock;

registro #(N) val1(clock,A,1'b0,out1);
registro #(N) val1(clock,B,1'b0,out2);

endmodule
